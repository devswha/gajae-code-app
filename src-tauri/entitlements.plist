<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    The Node.js sidecar (externalBin gajae-app-server) is signed with the
    hardened runtime by the Tauri bundler. V8 requires JIT and RWX memory,
    and packaged native addons (node-pty, better-sqlite3) are ad-hoc signed,
    so library validation must stay off. Without allow-jit the sidecar
    crashes with SIGTRAP ~20ms after spawn.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
