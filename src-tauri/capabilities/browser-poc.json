{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser-poc",
  "description": "PoC only: lets the served SPA on the supervised loopback origin open the unprivileged Built-in WebView browser window. Grants exactly the two PoC commands and nothing else; the browser window itself is referenced by no capability, so remote pages can invoke no commands.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*"
    ]
  },
  "permissions": [
    "allow-browser-poc-open",
    "allow-browser-poc-title-probe"
  ]
}
